server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: circuit
          uri: https://github.com/FlavioCesarps/microservice-demo
          search-paths: config-data
  application:
    name: config-service

